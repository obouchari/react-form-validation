/**
 * @author Otman Bouchari <obouchari@gmail.com>
 *
 * Available list of validation rules
 * accepted, url, email, after:date, afterOrEqual:date, alpha, alphaDash,
 * alphaNum, array, before:date, beforeOrEqual:date, between:min,max, bool,
 * confirmed:field, date, dateEqual:date, dateFormat:format, different:field,
 * range:min,max, distinct, image, in:array|string, notIn:array|string, min,
 * max, minLen, maxLen, required, numeric, regex:pattern, requiredIf:value,
 * requiredUnless:value, requiredIfSome:array, requiredIfAll:array,
 * timezone, same:value, size:string|array|number
 *
 * Note: Not all the rules are implemented at this moment.
 */

// export {default as required} from './required';
// export {default as minLen} from './minLen';
